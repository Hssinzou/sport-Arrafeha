import { MyFilterSearchPipe } from './my-filter-search.pipe';

describe('MyFilterSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new MyFilterSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
